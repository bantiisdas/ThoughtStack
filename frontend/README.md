# ThoughtStack frontend

Next.js (App Router) UI for ThoughtStack.

For setup, env vars, architecture, and the demo script, see the [root README](../README.md).

```bash
cp .env.example .env.local
npm install
npm run dev
```
