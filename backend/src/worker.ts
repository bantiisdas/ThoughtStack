/**
 * BullMQ worker entrypoint (Phase 2+).
 * Kept as a stub so `npm run worker` is available during scaffolding.
 */
import "dotenv/config";

console.log(
  "ThoughtStack worker stub — indexing worker will be implemented in Phase 2.",
);
console.log("Listening placeholder; exit with Ctrl+C.");

// Keep process alive in dev so the script doesn't immediately exit.
setInterval(() => {}, 60_000);
